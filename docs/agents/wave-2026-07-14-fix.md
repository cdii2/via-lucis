# Wave 2026-07-14 — FIX-A ∥ FIX-B (what-if audit remediation, first wave)

Source: `%TEMP%\via-lucis-whatif-audit-2026-07-14.md` §4. Baseline: main @ `a5b2f80`,
374/374 native tests. Repros live on branch `audit/whatif-throwaway` @ `9708a05`
(worktree `%TEMP%\vl-audit-wt`, dirs `test_whatif_audit`, `test_whatif_wave2`,
`test_whatif_wave3`).

Run order: FIX-A ∥ FIX-B now (disjoint file sets). FIX-C (MidiIo disconnect seam) and
DECIDE-D / PIN-E are LATER waves — not briefed here.

Merge order: FIX-A (wave lead) merges first, FIX-B rebases on it. Both pass the
via-lucis-dispatch REVIEW CHECKLIST before merge. Re-index codebase-memory after each
merge to main.

A-entry allocation (avoids parallel collisions in ASSUMPTIONS.md): FIX-A uses
**A89–A93**, FIX-B uses **A94–A97**. Lead reconciles numbering at merge if needed.

---

## BRIEF — FIX-A (wave lead): Scheduler/engine correctness pack

SESSION NAME: "Lucis · SchedFix". FIRST ACTION, before anything else: create an
isolated git worktree on a new branch `w7/sched` and do ALL your work there — other
agents are editing this repo in parallel and you must never share a working directory
with them.

### ROLE + GOAL
You are the wave lead fixing the scheduler/engine bug cluster from the 2026-07-14
what-if audit: 13 confirmed, repro-tested bugs (G1, G2, G4, G6, G7, G8, G9, G11, G12,
G17, G5, G3). Each fix lands TDD-style: bring the failing repro test from the audit
branch into the real suite, watch it fail, fix, and the test — flipped to assert the
fixed behavior — becomes the permanent pin. One audit item per commit.

### READ FIRST (in order)
1. `docs/SPEC.md` — LOCKED design.
2. `CLAUDE.md` (repo root) — iron rules, gates.
3. `PROGRESS.md` — current state; log your session in the Iteration log when done.
4. `%TEMP%\via-lucis-whatif-audit-2026-07-14.md` — the audit report, especially §1a,
   §1b, §1c, G17/G5/G3 rows, and §4 FIX-A. This is your work order.
5. The files you own (below) plus `echo_guard.h`.
6. The repro tests: branch `audit/whatif-throwaway` @ `9708a05` — **do not check that
   branch out in your worktree**; read/copy test files from it with
   `git show audit/whatif-throwaway:firmware/test/test_whatif_wave3/...` (list dirs
   with `git ls-tree -r --name-only audit/whatif-throwaway firmware/test/`).

### LOCKS
> Honor these LOCKED decisions (see `docs/SPEC.md` — do not relitigate):
> 1. **ESP32 does everything** — no PC/Pi/cloud in the loop; web UI served from the
>    device; songs = MIDI files on LittleFS. 2. **v1 features are fixed**: wait mode
>    (per-key chord clearing, hard red wrong-note flash), follow-along, tempo 1–500%,
>    hands separate, time-range loop, demo + accompaniment (device plays the piano over
>    MIDI), lookahead ramp. 3. **Ramp = exactly two variables**: one preview cap
>    (default 45%) + one lead time (default 1s); jump to 100% at press-time.
> 4. **Colors customizable; defaults L=blue R=green wrong=red** — defaults never
>    collide. 5. **No FP-30X-specific hacks in core** — piano quirks live behind one
>    small interface. 6. **Open source, MIT** — but `songs/local/` (copyrighted MIDIs)
>    is gitignored and NEVER committed or published. 7. OMR is a separate future
>    program; the MIDI-upload slot is the seam.

### YOU OWN
- `firmware/lib/core/src/vialucis/scheduler.cpp` / `scheduler.h`
- `firmware/lib/core/src/vialucis/playback_engine.cpp` / `playback_engine.h`
- `firmware/lib/core/src/vialucis/wait_mode.cpp` / `wait_mode.h`
- `firmware/lib/core/src/vialucis/note_emitter.cpp` / `note_emitter.h`
- `firmware/lib/core/src/vialucis/echo_guard.h`
- Test dirs: `firmware/test/test_scheduler`, `test_playback_engine`, `test_wait_mode`,
  `test_note_emitter` (additive to others only via new files — prefer not at all).
- `ASSUMPTIONS.md` entries **A89–A93 only**; your PROGRESS.md Iteration-log entry.

### YOU MUST NOT TOUCH
`mode_director.cpp/h`, `app.cpp`, `web_server.cpp`, `main.cpp`, `platformio.ini`, any
REST route or settings-schema definition, `firmware/test/helpers` (if a helper change
is unavoidable, make it strictly additive and flag it in your report — FIX-B is running
in parallel), anything under `webui/`, `songs/`.

Note: `EchoGuard guard_` and `setMode(...)` both live in PlaybackEngine — your echo
fixes (A-3) must NOT require edits to ModeDirector. If you discover they do, STOP that
item and report; do not edit ModeDirector yourself.

### WORK ITEMS (one commit each; repro test named per item in audit §1)
- **A-1 Loop/seek correctness** — G1: wrap guard at `pos_ == loopEnd_` must still fire
  after a barrier tie (make it inclusive-safe). G2: loop set entirely behind the
  playhead — DECIDE per SPEC (wrap into the loop vs clamp+finish), log A-entry. G4:
  replace the 64-iteration wrap guard with an O(1) modulo collapse for micro-loops.
  G6: `Scheduler::seek` clamps to duration.
- **A-2 Chord-progress preservation** — G7/G8/G9: `transport("play")` resume, no-op
  setTrack, and pause→resume onto an UNCHANGED barrier must not wipe
  `pending_`/`cleared_`; `applyMasks` skips resync when masks are unchanged.
- **A-3 Echo-credit hygiene** — G11/G12: clear guard credits on setMode away from an
  emitting mode and on seek. **G10 (same-pitch accompaniment eats the press) is a
  design decision — do NOT silently change it**: log the tradeoff (scope credits to
  the emit mask's pitches vs shorter window) as an A-entry marked "needs bring-up
  data"; no code change for G10.
- **A-4 Pedal-off flush** — G17: NoteEmitter tracks pedal-down per channel;
  `allOff` emits CC64=0 where down.
- **A-5 Repeat-cue loop clamp** — G5: while looping, skip fill windows whose onset ≥
  loopEnd (including the ramp-preview `onsetsBetween` path).
- **A-6 Loop-without-practiced-onset** — G3: DECIDE (refuse the loop PUT vs hold at
  loopEnd) per SPEC's "wait mode is the product"; log A-entry; implement your call.

### INTEGRATION CONTRACT
If you need a struct change, a new/changed REST route, a settings-schema change, a new
lib dep, or a main.cpp edit — you ARE the wave lead for this wave, but those are all
out of scope for this pack: STOP and report rather than expanding scope. FIX-B may ask
you (via the dispatcher) for a small engine API (e.g. show-state query); additive-only,
and note it in your report.

### IRON RULES
> 1. **Wait mode is the product** — keep BLE-in → match → LED-out lean; no per-frame
> allocations in the hot path. 2. **FastLED global power cap ALWAYS** — 2m of COB
> out-draws the 10A supply at full white. 3. **MIDI echo guard** — notes WE send to
> the piano must never count as user key presses. 4. **Core stays pure C++** — no
> Arduino headers in `firmware/lib/core/`; everything there must run under the native
> test env. 5. **Pins are FROZEN** (LED data = GPIO16) once BUILD-GUIDE.md exists —
> never move them. 6. **Replicability is a feature** — a stranger with the BOM and a
> browser must be able to flash and run; keep setup steps few and documented.
> 7. Web UI ships gzipped in flash and must leave room for songs.

### VERIFY (gate on every commit)
```
pio test -e native      # judge by output lines (grep FAIL/ERROR), not exit code alone
pio run -e esp32dev     # firmware must compile clean
git status              # confirm songs/local/ and secrets are NOT staged
```
Zero new allocation/blocking/indirection on the BLE-in → match → LED-out hot path.
Test count STRICTLY grows (baseline 374).

### DEFINITION OF DONE
Every G-item above either fixed+pinned (repro test now passing in the real suite) or
explicitly logged as a decision A-entry (G10). Suite ALL PASS with count > 374,
esp32dev clean, one commit per item, A89–A93 used as needed, PROGRESS.md Iteration log
updated. Final report: per-item status table, decisions made (with A-numbers), any
contract asks, anything FIX-B or a later wave must know.

---

## BRIEF — FIX-B: Director/show policy pack

SESSION NAME: "Lucis · ShowPolicy". FIRST ACTION, before anything else: create an
isolated git worktree on a new branch `w7/show` and do ALL your work there — other
agents are editing this repo in parallel and you must never share a working directory
with them.

### ROLE + GOAL
Fix the show/presentation policy holes from the 2026-07-14 what-if audit: G13, G14,
G15, G16 (confirmed, repro-tested) plus device-level D2 and D3, and the D4 comment
honesty fix. TDD: bring each failing repro from the audit branch into the real suite,
fix, flip to pin. One item per commit.

### READ FIRST (in order)
1. `docs/SPEC.md` — LOCKED design.
2. `CLAUDE.md` (repo root) — iron rules, gates.
3. `PROGRESS.md` — current state; log your session in the Iteration log when done.
4. `%TEMP%\via-lucis-whatif-audit-2026-07-14.md` — §1d, §2 rows D2/D3/D4, §4 FIX-B.
5. The files you own (below); read `playback_engine.h` and `show_player.h` as
   read-only consumers.
6. The repro tests: branch `audit/whatif-throwaway` @ `9708a05` — **do not check that
   branch out in your worktree**; read/copy test files from it with
   `git show audit/whatif-throwaway:firmware/test/...` (list with
   `git ls-tree -r --name-only audit/whatif-throwaway firmware/test/`). Yours: G13 =
   `test_s4_probe_refused_during_score_follow_show`, G14 = `test_s3_setmode_during_
   show_does_not_freeze_show_clock`, G15 = `test_w29_test_pattern_refused_or_inert_
   during_show`, G16 = `test_C22_test_pattern_freezes_scorefollow_clock`.

### LOCKS
> Honor these LOCKED decisions (see `docs/SPEC.md` — do not relitigate):
> 1. **ESP32 does everything** — no PC/Pi/cloud in the loop; web UI served from the
>    device; songs = MIDI files on LittleFS. 2. **v1 features are fixed**: wait mode
>    (per-key chord clearing, hard red wrong-note flash), follow-along, tempo 1–500%,
>    hands separate, time-range loop, demo + accompaniment (device plays the piano over
>    MIDI), lookahead ramp. 3. **Ramp = exactly two variables**: one preview cap
>    (default 45%) + one lead time (default 1s); jump to 100% at press-time.
> 4. **Colors customizable; defaults L=blue R=green wrong=red** — defaults never
>    collide. 5. **No FP-30X-specific hacks in core** — piano quirks live behind one
>    small interface. 6. **Open source, MIT** — but `songs/local/` (copyrighted MIDIs)
>    is gitignored and NEVER committed or published. 7. OMR is a separate future
>    program; the MIDI-upload slot is the seam.

### YOU OWN
- `firmware/lib/core/src/vialucis/mode_director.cpp` / `mode_director.h`
- `firmware/src/app.cpp` (D3, D4 scope only)
- `firmware/src/web_server.cpp` (D2 guard only)
- Test dir: `firmware/test/test_mode_director` (additive to others only via new
  files — prefer not at all).
- `ASSUMPTIONS.md` entries **A94–A97 only**; your PROGRESS.md Iteration-log entry.

### YOU MUST NOT TOUCH
`scheduler.*`, `playback_engine.*`, `wait_mode.*`, `note_emitter.*`, `echo_guard.h`
(FIX-A owns all of these in parallel RIGHT NOW — read-only for you), `main.cpp`,
`platformio.ini`, `model.h`/settings schema, `firmware/test/helpers` (additive-only if
unavoidable + flag in report), `webui/`, `songs/`. Do not touch any echo-credit logic
anywhere — that is FIX-A's A-3.

### WORK ITEMS (one commit each)
- **B-1 Probe × show** — G13: `armProbe` must also refuse while a show is playing,
  including score-follow shows where transport is deliberately stopped (the existing
  `Playing`-state refusal is the hole). Refusal is typed, matching the existing
  refusal pattern.
- **B-2 Test-pattern × show** — G15/G16: today the pattern hijacks a demo-clock show
  AND auto-pauses it, while under score-follow the clock keeps running beneath the
  pattern (breaks the A35 no-skipped-time guarantee). DECIDE one rule covering both
  clock kinds (refuse while show playing, or stop the show first), log A-entry,
  implement uniformly.
- **B-3 Mode/transport REST × show** — G14: `/api/mode` → wait during a demo-clock
  show freezes the show clock on stage; refuse or route through show teardown (align
  with your B-2 rule). D3: while a show is playing, `setMode` must not clobber
  `lastMode_`/`lastPractice_` (app.cpp:236-239) — a later show-stop must restore the
  player's real pre-show choice.
- **B-4 Shim fixes** — D2: DELETE route refuses (or safely unloads first) when the
  target song is the currently-loaded one (web_server.cpp). D4: correct the
  `frameDirty_` "benign race" comment in `playback_engine.h:218-222` — **exception to
  your no-touch list: this one comment block only, no code lines** (the real race is
  HTTP-task REST writes vs `frameDue()`, not the BLE path). Coordinate at merge: it
  rebases trivially since FIX-A merges first.

Policy-logic placement: `app.cpp`/`web_server.cpp` are structurally untested device
shims — put every decidable rule (refusals, show-state checks, lastMode preservation
logic) in ModeDirector where the native suite can pin it; the shim stays a thin
pass-through.

REST contract: NO new routes, no changed response shapes for currently-accepted
requests — new refusals only, in the existing typed-refusal shape. If any refusal is
user-visible in normal operation, update the relevant doc (TROUBLESHOOTING /
SHOW-FORMAT) in this same wave.

### INTEGRATION CONTRACT
If you need a struct change, a new/changed REST route, a settings-schema change, a new
lib dep, or a main.cpp edit, and you are NOT the wave lead: STOP and report — do not
make that change yourself. Same rule if a fix turns out to require edits inside
PlaybackEngine/Scheduler: report the needed API (e.g. a show-state query) instead of
adding it.

### IRON RULES
> 1. **Wait mode is the product** — keep BLE-in → match → LED-out lean; no per-frame
> allocations in the hot path. 2. **FastLED global power cap ALWAYS** — 2m of COB
> out-draws the 10A supply at full white. 3. **MIDI echo guard** — notes WE send to
> the piano must never count as user key presses. 4. **Core stays pure C++** — no
> Arduino headers in `firmware/lib/core/`; everything there must run under the native
> test env. 5. **Pins are FROZEN** (LED data = GPIO16) once BUILD-GUIDE.md exists —
> never move them. 6. **Replicability is a feature** — a stranger with the BOM and a
> browser must be able to flash and run; keep setup steps few and documented.
> 7. Web UI ships gzipped in flash and must leave room for songs.

### VERIFY (gate on every commit)
```
pio test -e native      # judge by output lines (grep FAIL/ERROR), not exit code alone
pio run -e esp32dev     # firmware must compile clean
git status              # confirm songs/local/ and secrets are NOT staged
```
Test count STRICTLY grows (baseline 374; FIX-A is growing it in parallel — your
comparison is against your own branch point).

### DEFINITION OF DONE
G13–G16 fixed+pinned (repros passing in `test_mode_director`), D2 guarded, D3 fixed,
D4 comment corrected, decisions logged as A94–A97, docs updated if behavior is
user-visible, suite ALL PASS, esp32dev clean, one commit per item, PROGRESS.md
Iteration log updated. Final report: per-item status, decisions (A-numbers), any
engine-API asks that had to be deferred to the lead.

---

## Mechanics
- Spawn both with the Agent tool, `isolation: "worktree"` NOT needed (briefs create
  their own worktrees as first action) — but `run_in_background: true` for FIX-B;
  iterate via SendMessage, never re-spawn cold.
- Builder model override: FIX-A = **opus** (scheduler/engine C++ is the
  precision-critical slice), FIX-B = **sonnet**.
- ASSUMPTIONS.md and PROGRESS.md will conflict at merge (both append) — lead resolves;
  A-number blocks prevent semantic collisions.

## Merge order (each merge passes the REVIEW CHECKLIST first)
1. FIX-A → main (wave lead; scheduler/engine chokepoint lands first).
2. FIX-B rebased on new main → main (its D4 comment edit and any test-count deltas
   reconcile here).
3. Re-index codebase-memory (`fast`) after each merge; delete the audit worktree/branch
   only after BOTH merges (repros are still the source of truth until moved).
4. FIX-C (MidiIo disconnect seam) is the NEXT wave, lead-only — do not start it here.
