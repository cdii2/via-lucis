# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Node (webui tooling, if any)
node_modules/
dist/

# Editors / OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# Agent worktrees / harness scratch
.claude/

# Local scratch
*.local.*

# Gate logs — the documented ritual (docs/SIMULATOR.md "don't trust the exit code")
# tees `pio test` output to a log file, so running the gate drops one in the tree.
*.log

# Downloaded MIDIs (copyright-unclear) — NEVER commit; see PROGRESS.md
songs/local/
.gstack/

__pycache__/
